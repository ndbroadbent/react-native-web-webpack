machine:
  node:
    version: 7.8.0
  npm:
    version: 5.0.3

dependencies:
  override:
    - npm install

test:
  override:
    - npm test

deployment:
  gh-pages:
    branch: master
    commands:
      - npm run publish
